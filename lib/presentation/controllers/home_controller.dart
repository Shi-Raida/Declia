import 'package:get/get.dart';

class HomeController extends GetxController {
  HomeController();
  // Use cases will be injected here via constructor parameters
  // e.g. HomeController(this._authenticateUser);
}
